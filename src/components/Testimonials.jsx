import React from 'react'

const Testimonials = ({text}) => {
  return (
    <div className='text-lg font-semibold p-10 px-20 flex justify-center items-center'>
        <p>{text}</p>
    </div>
  )
}

export default Testimonials